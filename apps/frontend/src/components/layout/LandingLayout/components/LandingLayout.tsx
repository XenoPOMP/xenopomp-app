import type { FC, PropsWithChildren } from 'react';

// eslint-disable-next-line jsdoc/require-jsdoc
export const Internal_LandingLayout: FC<PropsWithChildren> = ({ children }) => {
  return <>{children}</>;
};
