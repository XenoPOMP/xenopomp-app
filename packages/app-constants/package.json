{
  "name": "@repo/constants",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "yarn tsup --watch",
    "build": "yarn tsup",
    "lint": "eslint",
    "tsup": "tsup src/index.ts --format cjs,esm --dts --target es2022 --clean"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "devDependencies": {
    "@repo/eslint-config": "*",
    "@repo/ts-config": "*"
  }
}